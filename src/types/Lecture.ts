export type Lecture = {
    id: string
    graduate: string
    semester: string
    title: string
    year: string
}

export type LectureRequest = {
    graduate: string
    semester: string
    title: string
    year: string
}
